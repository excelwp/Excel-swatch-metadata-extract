# Wallpaper Metadata Extractor (Claude + Neon + Streamlit)

## Run locally
1. Create .env with:
- ANTHROPIC_API_KEY=...
- DATABASE_URL=...

2. Install:
pip install -r requirements.txt

3. Run:
streamlit run app.py
"# wallpaper-swatch-metadata-claude" 
